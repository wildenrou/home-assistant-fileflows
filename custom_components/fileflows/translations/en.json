{
  "config": {
    "step": {
      "user": {
        "title": "FileFlows",
        "description": "Connect a FileFlows instance.",
        "data": {
          "name": "Name",
          "url": "URL",
          "scan_interval": "Poll Interval",
          "timeout": "Timeout",
          "connected_last_seen_timespan": "Show as disconnected when node not seen in"
        }
      }
    },
    "error": {
      "connection_failed": "Unable to connect."
    }
  }
}
